package org.jetbrains.exposed.example



object A10DSL {

    /**
     * Where表达式
     *
     * Query expression (where) expects a boolean operator (ie: Op<Boolean>
     * ```
     * eq - (==)
     * neq - (!=)
     * isNull()
     * isNotNull()
     * less - (<)
     * lessEq - (<=)
     * greater - (>)
     * greaterEq - (>=)
     * like - (=~)
     * notLike - (!~)
     * exists
     * notExists
     * regexp
     * notRegexp
     * inList
     * notInList
     * between
     * match (MySQL MATCH AGAINST)
     * ```
     *
     * 逻辑条件:
     * ```
     * not
     * and
     * or
     * ```
     *
     */
    fun main() = Unit

}

fun main() = A10DSL.main()
